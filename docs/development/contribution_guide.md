# Contribution Guide

To begin development:
1. Run `python scripts/setup.py` to validate packages and run checks.
2. Edit configuration options in `.env`.
3. Add your changes and run `pytest` to verify test suite coverage.
