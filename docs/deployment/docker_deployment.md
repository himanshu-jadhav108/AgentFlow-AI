# Docker Deployment Guide

To deploy using Docker:
1. Ensure Docker and Docker Compose are installed.
2. Run `docker compose up --build` to compile images and start containers.
3. Check status via `GET /system/status`.
